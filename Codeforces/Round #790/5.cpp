#include <bits/stdc++.h>

using namespace std;

long long q;

void solution(){
    int n, m;
    cin>>n>>m;

    int a[n];

    int total = 0;
    for(int i=0; i<n; i++){
        cin>>a[i];
        total+=a[i];
    }

    for(int i=0; i<m; i++){
        int t; cin>>t;
        

    }

    return;
}

int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    cin>>q;

    while(q--){
        solution();
    }

    return 0;
}