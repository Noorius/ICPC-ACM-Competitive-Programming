#include <bits/stdc++.h>

//#define N 2*10e2
#define ll long long
using namespace std;

void solution(){
    string now, need;
    cin>>now>>need;

    cout<<now.substr(0,2);
}


int main(){
    // freopen("bad.in", "r", stdin);
    // freopen("bad.out", "w", stdout);

    solution();


    return 0;
}